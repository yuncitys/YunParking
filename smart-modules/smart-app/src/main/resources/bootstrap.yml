spring:
  cloud:
    config:
      discovery:
        service-id: smart-config
        enabled: true
      label: master  # git 分支
      name: app
      profile: dev  #dev，test，prod
      uri: http://localhost:8750
